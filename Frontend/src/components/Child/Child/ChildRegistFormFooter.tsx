import Button from '@components/Shared/Button'

export default function ChildRegistFormFooter() {
  return (
    <Button full onClick={() => {}} color="bg-lightgreen">
      <strong className="text-white">원생 등록</strong>
    </Button>
  )
}
